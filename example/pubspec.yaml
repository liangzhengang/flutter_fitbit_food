name: fitbitter_example
description: A simple app that demonstrates the use of fitbitter.

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  # ------- FITBITTER 
  fitbitter:
    path: ../
  # --------------------


dev_dependencies:
  flutter_test:
    sdk: flutter
  
  # ----- MOOR 
  moor_generator: # use the latest version
  build_runner: 
  # ----- 

# The following section is specific to Flutter.
flutter:
  uses-material-design: true

